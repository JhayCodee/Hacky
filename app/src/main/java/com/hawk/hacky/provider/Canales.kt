package com.hawk.hacky.provider

data class Canales (

    var categoria:String ? = null,
    var descripcion:String ? = null,
    var img:String ? = null,
    var link:String ? = null,
    var nombre:String ? = null,

)